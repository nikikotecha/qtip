accelerate>=1.0,<2
cuda_python==12.6.0
datasets>=2.20.0,<4
glog==0.3.1
huggingface_hub>=0.30
lm_eval==0.4.4
numpy>=2.1,<3
scipy>=1.14
setuptools>=69.5.1
tqdm>=4.66
# Qwen3 model_type needs transformers >= 4.51 (upstream pin 4.45.2 predates it).
# Vendored model/qwen3*.py imports legacy transformers internals
# (_prepare_4d_causal_attention_mask, ALL_LAYERNORM_LAYERS, ...) that are
# removed in newer releases, so cap below 4.56.
transformers==4.51.3
# Not pip-resolvable extras (install manually, in this order, AFTER torch):
#   torch >= 2.4 (use the image's CUDA build; fork validated on torch 2.6)
#   pip install fast-hadamard-transform          (builds CUDA ext from source)
#   cd qtip-kernels && pip install --no-build-isolation -e .   (needs sm_80+)
